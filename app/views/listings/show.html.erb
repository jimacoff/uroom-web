<h1>Listings#show</h1>
<p>Find me in app/views/listings/show.html.erb</p>
<h2><%= @listing %></h2>
<h2><%= button_to 'Orbit', {:action => "orbit", :id => @id,:month => @month, :year => @year, :lease_length => @lease_length, :roommates => params[:roommates]}   %></h2>
